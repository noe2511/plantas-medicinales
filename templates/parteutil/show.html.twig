{% extends 'base.html.twig' %}

{% block title %}Parteutil{% endblock %}

{% block body %}
    <h1>Parteutil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id parte útil</th>
                <td>{{ parteutil.idparteutil }}</td>
            </tr>
            <tr>
                <th>Parte</th>
                <td>{{ parteutil.parte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('parteutil_index') }}">Volver a la lista de partes útiles</a>

    <a href="{{ path('editarParteUtil', {'idparteutil': parteutil.idparteutil}) }}">Editar</a>

    {{ include('parteutil/borrarParteUtil.html.twig') }}
{% endblock %}
