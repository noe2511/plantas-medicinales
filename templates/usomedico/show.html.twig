{% extends 'base.html.twig' %}

{% block title %}Usomedico{% endblock %}

{% block body %}
    <h1>Usomedico</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idusomedico</th>
                <td>{{ usomedico.idusomedico }}</td>
            </tr>
            <tr>
                <th>Uso</th>
                <td>{{ usomedico.uso }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('usomedico_index') }}">back to list</a>

    <a href="{{ path('usomedico_edit', {'idusomedico': usomedico.idusomedico}) }}">Editar</a>

    {{ include('usomedico/_delete_form.html.twig') }}
{% endblock %}
