<form method="post" action="{{ path('usomedico_delete', {'idusomedico': usomedico.idusomedico}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usomedico.idusomedico) }}">
    <button class="btn">Borrar</button>
</form>
