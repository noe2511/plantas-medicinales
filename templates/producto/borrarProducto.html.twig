<form method="post" action="{{ path('borrarProducto', {'idproducto': producto.idproducto}) }}" onsubmit="return confirm('¿Está seguro que quiere borrar el producto?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.idproducto) }}">
    <button class="btn">Borrar</button>
</form>
